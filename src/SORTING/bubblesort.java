package SORTING;

public class bubblesort {
    // main idea behind bubble sort is that it checks each adjacent elem and pushes the larger element towards the end
    // at each pass the largest elem is at its right position = sorted

}
